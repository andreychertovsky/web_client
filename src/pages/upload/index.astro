---
import { BaseLayout } from "@/layouts";
import { containerStyles } from "@/shared/ui/container";
import { NavDesktop, NavMobile } from "@/shared/ui/nav";
import { Dialog } from "@/shared/ui/dialog";
import { DIALOGS } from "@/shared/config";

import { Footer } from "../_ui/footer";

import { UploadForm } from "./_ui/upload-form";
import styles from "./_styles.module.css";
---

<BaseLayout>
  <header class={containerStyles}>
    <NavDesktop />
  </header>
  <main class={styles.main}>
    <section class:list={[containerStyles, styles.container]}>
      <h1 class={styles.heading}>Search any products</h1>
      <UploadForm />
    </section>
  </main>
  <Footer />
  <Dialog name={DIALOGS.MENU}>
    <NavMobile />
  </Dialog>
</BaseLayout>
